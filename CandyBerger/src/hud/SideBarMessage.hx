package hud;

@:build(defold.support.MessageBuilder.build())
class SideBarMessage {
	var set_level:{amount:Int};
	var set_score:{amount:Int};
	var set_options:{amount:Int};
	var set_lives:{amount:Int};
}
