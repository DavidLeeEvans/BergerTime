@:build(defold.support.MessageBuilder.build())
class Messages {
	var start_game;
	var start_level:{difficulty:Int};
	var next_level;
	var restart_level;
	var to_main_menu;
	var pause_off;
	var pause_on;
	var exit_game;
	var settings;
}
